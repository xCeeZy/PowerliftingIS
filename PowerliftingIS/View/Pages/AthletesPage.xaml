<Page x:Class="PowerliftingIS.View.Pages.AthletesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PowerliftingIS.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="{StaticResource BgDeep}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Style="{StaticResource PageTitle}"
                   Text="Спортсмены"/>

        <Border Grid.Row="1"
                Style="{StaticResource TitleAccentBar}"/>

        <!-- Панель фильтров и кнопок -->
        <Grid Grid.Row="2"
              Margin="0 0 0 14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     x:Name="SearchTb"
                     Style="{StaticResource SearchTextBox}"
                     Width="200"
                     TextChanged="SearchTb_TextChanged"/>

            <ComboBox Grid.Column="2"
                      x:Name="RankFilterCb"
                      Style="{StaticResource InputComboBox}"
                      Width="160"
                      SelectionChanged="RankFilterCb_SelectionChanged"/>

            <Button Grid.Column="4"
                    Style="{StaticResource PrimaryButton}"
                    Content="+ Добавить"
                    Width="120"
                    Click="AddBtn_Click"/>

            <Button Grid.Column="6"
                    Style="{StaticResource DangerButton}"
                    Content="Удалить"
                    Width="100"
                    Click="DeleteBtn_Click"/>
        </Grid>

        <!-- Таблица -->
        <DataGrid Grid.Row="3"
                  x:Name="AthletesDg"
                  Style="{StaticResource MainDataGrid}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО"
                                    Binding="{Binding FullName}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Дата рождения"
                                    Binding="{Binding BirthDate, StringFormat='dd.MM.yyyy'}"
                                    Width="120"/>
                <DataGridTextColumn Header="Разряд"
                                    Binding="{Binding Ranks.RankName}"
                                    Width="120"/>
                <DataGridTextColumn Header="Весовая категория"
                                    Binding="{Binding WeightCategories.CategoryName}"
                                    Width="140"/>
                <DataGridTextColumn Header="Тренер"
                                    Binding="{Binding Coaches.FullName}"
                                    Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>

